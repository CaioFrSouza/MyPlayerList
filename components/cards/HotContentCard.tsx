import React from 'react';
import { StyleSheet } from 'react-native';
import { IHotContentCard } from '../Interfaces/IHotContentCard';

const HotContentCard: React.FC<IHotContentCard> = (props) => {
    return <>
    
    </>
}

const styles = StyleSheet.create({

})